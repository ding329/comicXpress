{{#em-form model=registration action="undefined"}}
	{{em-input property="username" placeholder="Enter a username"}}
	{{em-input property="password" placeholder="Enter a password" type=password}}
	{{em-input property="storeName" placeholder="Enter Store Name"}}
	{{em-input property="email" placeholder="Enter Email"}}
{{/em-form}}

